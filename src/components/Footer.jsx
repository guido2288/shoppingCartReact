import { useCart } from '../hooks/useCart'
import './Footer.css'
export const Footer = ({ filters }) => {

  const { cart } = useCart()

  return (
    <footer className='footer'>

    </footer>
  )
}
